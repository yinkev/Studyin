/**
 * Dashboard - Bento Grid Layout with HeroUI
 * Ocean Breeze Theme with Color Variety
 */

'use client';

import { Card, CardBody, CardHeader, Progress, Chip, Avatar, Button } from '@heroui/react';

export default function DashboardPage() {
  return (
    <div className="min-h-screen" style={{
      background: '#F5F5F5',
    }}>
      {/* Header */}
      <header className="border-b" style={{
        background: '#FFFFFF',
        borderColor: '#E5E7EB',
      }}>
        <div className="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 rounded-xl flex items-center justify-center" style={{
              background: '#3B82F6',
              boxShadow: '0 4px 0 #2563EB',
            }}>
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="white" strokeWidth="2">
                <path d="M12 2L2 7l10 5 10-5-10-5z"/>
                <path d="M2 17l10 5 10-5"/>
                <path d="M2 12l10 5 10-5"/>
              </svg>
            </div>
            <h1 className="text-2xl font-bold" style={{
              color: '#111827',
            }}>
              Dashboard
            </h1>
          </div>
          <div className="flex items-center gap-3">
            <div className="text-right">
              <div className="text-sm font-medium text-gray-900">Level 7</div>
              <div className="text-xs text-gray-600">2,450 XP</div>
            </div>
            <Avatar
              src=""
              className="w-10 h-10"
              style={{
                background: '#3B82F6',
              }}
            />
          </div>
        </div>
      </header>

      {/* Main Content - Bento Grid */}
      <main className="max-w-7xl mx-auto px-6 py-8">
        {/* Bento Grid Layout */}
        <div className="grid grid-cols-12 gap-4">

          {/* Large Card - Today's Progress */}
          <Card className="col-span-12 md:col-span-8 bg-white" style={{
            borderRadius: '32px',
            boxShadow: '0 4px 0 #E5E7EB, 0 12px 24px rgba(0, 0, 0, 0.08)',
            border: 'none',
          }}>
            <CardHeader className="flex gap-3 items-center">
              <div className="p-2 rounded-xl" style={{ background: '#F3F4F6' }}>
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#6B7280" strokeWidth="2">
                  <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                  <polyline points="22 4 12 14.01 9 11.01"/>
                </svg>
              </div>
              <div className="flex flex-col">
                <p className="text-lg font-bold" style={{ color: '#111827' }}>Today's Progress</p>
                <p className="text-sm" style={{ color: '#6B7280' }}>Keep up the momentum!</p>
              </div>
            </CardHeader>
            <CardBody className="space-y-6 pt-2">
              <div>
                <div className="flex justify-between mb-2">
                  <span className="text-sm font-medium" style={{ color: '#374151' }}>Questions Answered</span>
                  <span className="text-sm font-bold" style={{ color: '#111827' }}>23 / 30</span>
                </div>
                <Progress
                  value={76}
                  className="h-3"
                  style={{ background: '#F3F4F6' }}
                />
              </div>
              <div>
                <div className="flex justify-between mb-2">
                  <span className="text-sm font-medium" style={{ color: '#374151' }}>Study Time</span>
                  <span className="text-sm font-bold" style={{ color: '#111827' }}>45 min / 60 min</span>
                </div>
                <Progress
                  value={75}
                  className="h-3"
                  style={{ background: '#F3F4F6' }}
                />
              </div>
              <div>
                <div className="flex justify-between mb-2">
                  <span className="text-sm font-medium" style={{ color: '#374151' }}>Accuracy</span>
                  <span className="text-sm font-bold" style={{ color: '#111827' }}>87%</span>
                </div>
                <Progress
                  value={87}
                  className="h-3"
                  style={{ background: '#F3F4F6' }}
                />
              </div>
            </CardBody>
          </Card>

          {/* Streak Card */}
          <Card className="col-span-6 md:col-span-4 bg-white" style={{
            borderRadius: '32px',
            boxShadow: '0 4px 0 #E5E7EB, 0 12px 24px rgba(0, 0, 0, 0.08)',
            border: 'none',
          }}>
            <CardBody className="flex flex-col items-center justify-center p-6">
              <div className="text-6xl font-bold mb-2" style={{
                color: '#111827',
              }}>
                12
              </div>
              <p className="text-sm font-semibold mb-1" style={{ color: '#6B7280' }}>Day Streak</p>
              <Chip
                variant="flat"
                size="sm"
                style={{ background: '#F3F4F6', color: '#374151' }}
              >
                🔥 On Fire!
              </Chip>
            </CardBody>
          </Card>

          {/* XP Card */}
          <Card className="col-span-6 md:col-span-4 bg-white" style={{
            borderRadius: '32px',
            boxShadow: '0 4px 0 #E5E7EB, 0 12px 24px rgba(0, 0, 0, 0.08)',
            border: 'none',
          }}>
            <CardBody className="flex flex-col items-center justify-center p-6">
              <div className="text-6xl font-bold mb-2" style={{
                color: '#111827',
              }}>
                2.4K
              </div>
              <p className="text-sm font-semibold mb-1" style={{ color: '#6B7280' }}>Total XP</p>
              <Chip
                style={{ background: '#F3F4F6', color: '#374151' }}
                size="sm"
              >
                ⭐ Level 7
              </Chip>
            </CardBody>
          </Card>

          {/* Mastery Card */}
          <Card className="col-span-6 md:col-span-4 bg-white" style={{
            borderRadius: '32px',
            boxShadow: '0 4px 0 #E5E7EB, 0 12px 24px rgba(0, 0, 0, 0.08)',
            border: 'none',
          }}>
            <CardBody className="flex flex-col items-center justify-center p-6">
              <div className="text-6xl font-bold mb-2" style={{
                color: '#111827',
              }}>
                82%
              </div>
              <p className="text-sm font-semibold mb-1" style={{ color: '#6B7280' }}>Avg Mastery</p>
              <Chip
                variant="flat"
                size="sm"
                style={{ background: '#F3F4F6', color: '#374151' }}
              >
                ✓ Excellent
              </Chip>
            </CardBody>
          </Card>

          {/* Quick Actions */}
          <Card className="col-span-6 md:col-span-4 bg-white" style={{
            borderRadius: '32px',
            boxShadow: '0 4px 0 #E5E7EB, 0 12px 24px rgba(0, 0, 0, 0.08)',
            border: 'none',
          }}>
            <CardHeader className="pb-2">
              <p className="text-sm font-bold" style={{ color: '#111827' }}>Quick Start</p>
            </CardHeader>
            <CardBody className="pt-0 space-y-3">
              <Button
                fullWidth
                size="lg"
                style={{
                  background: '#3B82F6',
                  color: 'white',
                  borderRadius: '50px',
                  boxShadow: '0 6px 0 #2563EB',
                  fontWeight: '700',
                }}
              >
                📚 Resume Study
              </Button>
              <Button
                fullWidth
                size="lg"
                variant="bordered"
                style={{
                  borderColor: '#E5E7EB',
                  color: '#374151',
                  borderRadius: '50px',
                  borderWidth: '2px',
                  fontWeight: '600',
                }}
              >
                📤 Upload Content
              </Button>
            </CardBody>
          </Card>

          {/* Recent Topics */}
          <Card className="col-span-12 md:col-span-8 bg-white" style={{
            borderRadius: '32px',
            boxShadow: '0 4px 0 #E5E7EB, 0 12px 24px rgba(0, 0, 0, 0.08)',
            border: 'none',
          }}>
            <CardHeader>
              <div className="flex gap-3 items-center">
                <div className="p-2 rounded-xl" style={{ background: '#F3F4F6' }}>
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#6B7280" strokeWidth="2">
                    <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/>
                    <path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
                  </svg>
                </div>
                <p className="text-lg font-bold" style={{ color: '#111827' }}>Recent Topics</p>
              </div>
            </CardHeader>
            <CardBody>
              <div className="space-y-3">
                {[
                  { name: 'ACE Inhibitors', mastery: 89 },
                  { name: 'Beta Blockers', mastery: 76 },
                  { name: 'Diuretics', mastery: 82 },
                ].map((topic, i) => (
                  <div key={i} className="flex items-center justify-between p-4" style={{
                    background: '#F9FAFB',
                    borderRadius: '20px',
                    border: '2px solid #E5E7EB',
                  }}>
                    <span className="font-semibold" style={{ color: '#374151' }}>{topic.name}</span>
                    <div className="flex items-center gap-3">
                      <Progress
                        value={topic.mastery}
                        className="w-32 h-2"
                        style={{ background: '#E5E7EB' }}
                      />
                      <span className="text-sm font-bold w-12 text-right" style={{ color: '#111827' }}>
                        {topic.mastery}%
                      </span>
                    </div>
                  </div>
                ))}
              </div>
            </CardBody>
          </Card>

        </div>
      </main>
    </div>
  );
}
