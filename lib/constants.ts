// Central app-wide constants

// LocalStorage keys
export const LS_KEYS = {
  THEME: 'studyin-theme',
  XP_PROGRESS: 'studyin-xp-progress',
  LEARNER_ID: 'studyin:learnerId',
} as const;

// Development base URL used by tests and local tooling
export const DEV_BASE_URL = 'http://localhost:3005';

// Default learner id for local/dev flows
export const DEFAULT_LEARNER_ID = 'local-dev';

